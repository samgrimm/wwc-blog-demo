<h1><%= t('adding') %> Post</h1>

<%= form_for(@post, url:posts_path( locale: I18n.locale) ) do |f| %>

  <%= render 'form',  f: f %>

  <div class="form-group">
    <%= f.submit t('.create'), class:'btn btn-primary' %>
  </div>
  <% end %>

<%= link_to fa_icon('arrow-circle-left 2x'), posts_path(locale:I18n.locale) %>
